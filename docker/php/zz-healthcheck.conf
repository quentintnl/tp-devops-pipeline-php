; Active un endpoint de ping sur le pool php-fpm `www`.
; Permet au HEALTHCHECK de vérifier que le pool répond (FastCGI), sans toucher
; au code applicatif ni aux dépendances (DB/Redis).
[www]
ping.path = /ping
ping.response = pong
